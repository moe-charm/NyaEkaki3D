# 9855d43レビューの現行main再照合（bd30224）

レビュー基準 `9855d43` の指摘を、改名後リポジトリの現行 `main` `bd30224` へ再照合した。P1 3件とP2 5件は後続修正および回帰で解消済みであり、今回のレビューを理由に本番コードへ重複修正は加えていない。

| 指摘 | 現行契約 | 確認 |
|---|---|---|
| avatar移動後の衣装配置 | avatar-root localへ一度だけ親子付け | Unity Bridgeのtranslation／rotation／scale fixture |
| `SaveBindings`後のownership参照 | ObjectId assignmentと生成object参照を分離 | 同一ObjectIdのStateHash更新、削除後の割当読込 |
| UV1の欠落 | Windows v1はUV0。`TEXCOORD_1`を`UNSUPPORTED_UV_SET`で明示停止 | Core import／semantic texture／GLB export回帰 |
| sparse material slot | authored slotとdense submeshの対応を保持 | Core materialization／出力回帰 |
| MR scalar係数 | 画像チャンネルへmetallic／roughness係数を適用 | Player／Bridge semantic map回帰 |
| Cuff winding | 面の頂点順を修正 | 片面材質のtopology回帰 |
| sampler共有 | image共有とsampler variant共有を分離 | Repeat／Clamp出力回帰 |
| 適用前・削除後の割当読込 | assignment identityは生成objectの有無から独立 | `NyaForgeSkinnedClothingBinding`／Bridge回帰 |

現行Coreは **500 passed / 0 failed**（`C:/Users/tomoaki/AppData/Local/Temp/NyaForge-Core-Tests-50e9228e6a0741478e09cb624f919963`）。これはコードと合成fixtureの再確認であり、実EditorWindowのマウス／DPI、実RadDollV3へ新規衣装を全周fitした貫通・見た目、VRChat Build & Test／実機表示の受入を含まない。後続作業はこの手動境界を対象にする。
