# f010146フィードバックの現行HEAD再照合（2026-09-14）

提示された `f010146` 基準の衣装・材質フィードバックを、現行 `main` **`403078b`**へ再照合した。P1 3件とP2 5件は、`f010146`以降の実装と回帰で解消済みだったため、本番コードの重複修正は行っていない。

| 指摘 | 現行の扱い | 根拠 |
|---|---|---|
| MCP一括操作で保護objectを編集できる | batch内の選択変更を追跡し、各変更操作の対象objectを保護検査する | `AuthoringWorkbench.ReferenceProtection.cs`、MCP batch回帰 |
| 単一objectの汎用出力が保護を通す | 納品対象を先に解決し、GLB／Bakeの全入口で保護検査する | `AuthoringWorkbench.McpExport.cs`、`ProjectActions.cs`、`ProjectExportService.ExportObject` |
| Polygon→skinの疎なslotをPlayerが扱えない | 派生時にauthored slotをdense submeshへ正規化し、未使用slotのedgeを除去する | `AccessorySkinBindingAdapter.cs`、sparse slot／GLB回帰 |
| BridgeのMR係数が未反映 | glTFのB×metallicFactor、1−G×roughnessFactorをStandard shader用画像へ焼き込み、scalar二重適用を避ける | `SkinnedClothingReceiver.cs`、Unity Bridge semantic MR回帰 |
| 別Paintの原画像へ誤差し替え | `PaintNodeId`を優先し、preview hashは互換fallbackに限定する | `PaintEvaluation.cs`、`GlbExportService.cs`のnode identity回帰 |
| 複数objectから1個だけ納品すると失敗 | project全体ではなく解決済み納品対象の件数で出力分岐する | `ProjectActions.cs`、allowlist 1対象回帰 |
| 高DPIでwindow変更に追従しない | 通常UIのGeometryChangedEventでroot boundsを再計算する | `AuthoringWorkbench.cs`、DPI bounds回帰 |
| fit対象IDが別衣装へ持ち越される | object切替時にfit inspection／選択面／選択頂点を解除し、所有objectを記録する | `AuthoringWorkbench.Objects.cs`、fit ownership回帰 |

## 検証

- Core: **506 passed / 0 failed**。artifact `C:/Users/tomoaki/AppData/Local/Temp/NyaForge-Core-Tests-1b2721932b4e4843bac3ce68ab912291`。
- Windows Player: `Builds/PerformanceV33/NyaForge.exe` のAuthoring suiteと実RadDollV3単一候補Save/Open・VRM1出力がPASS。
- Unity Bridge: Unity `2022.3.22f1`のsemantic texture／clothing package受け取りがPASS。最新reportは `Artifacts/BridgeReceiver-20260914-095658-584-c6a32392123a4242b33fd1003392d2d0/bridge-report.json`。

この再照合はCore、Windows Player、Unity Bridge、自動化された実アバター経路を対象にしたもの。実EditorWindowのマウス／IME／Explorer操作、実アバター全周fitの貫通ゼロ・見た目、VRChat SDK Build & Test／実機表示は未受入として残る。実RadDollV3候補取込は約260.8秒、観測working setピーク約2.8GBで、軽量性は継続課題として扱う。
