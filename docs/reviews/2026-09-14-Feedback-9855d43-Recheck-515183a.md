# 9855d43フィードバックの現行HEAD再照合（2026-09-14）

提示された`9855d43`基準のP1 3件／P2 5件を、現行`main` **`515183a`**へ再照合した。今回の指摘は現行HEADより前の実装状態に対するもので、同じ修正を重ねて入れる必要はない。

| 指摘 | 現行の扱い | 根拠 |
|---|---|---|
| avatar移動後の衣装配置 | avatar-local座標を保持してreceiver rootへ一度だけ親子付け | `SkinnedClothingReceiver`、移動・回転・scale付きBridge回帰 |
| 更新時に旧衣装の管理参照を失う | assignment identity（ObjectId）と生成object参照を分離し、StateHash更新でも参照を保持 | `SkinnedClothingPackageWindow.SaveBindings`、ownership更新回帰 |
| UV1を受け付けて頂点データを落とす | Windows v1はUV0のみ。`TEXCOORD_1`は取込・semantic slot・GLB出力で`UNSUPPORTED_UV_SET`として明示停止 | `GlbImport`／`GlbMaterialSource`／`GlbExportService`、Core回帰 |
| sparse material slotの入替 | 使用slotと入力portを保持して派生・出力 | material slot回帰 |
| MR画像がscalar係数を無視 | Player shaderで画像値へmetallic／roughness係数を適用 | semantic mapのPlayer／Bridge回帰 |
| Cuffの面向き | primitive windingを修正し、片面材質の回帰を保持 | `PolygonPrimitives.Cuff`、Cuff topology回帰 |
| 共有画像＋異なるsamplerの統合 | image bytesとsampler hashを分離してtextureを共有しない | sampler variant回帰 |
| 適用前・削除後の割当読込 | assignment identityと生成物の存在判定を分離 | `NyaForgeSkinnedClothingBinding`、Bridge ownership回帰 |

## 現行HEADでの自動確認

- `dotnet run --project Tests/Authoring.Core/Authoring.Core.Tests.csproj --no-restore`: **501 passed / 0 failed**。artifact: `C:/Users/tomoaki/AppData/Local/Temp/NyaForge-Core-Tests-0773c4378c4745aba2e7740d364f327a`
- 既存のReleaseCandidateV2 Player、private RadDollV3取込・Save/Open・GLB／VRM1 smoke、Unity 2022.3.22f1 Bridge受け取りのPASS証拠は`current_task.md` NF-V1-06へ記録済み。

この再照合で本番コードの重複修正は行っていない。残るのは、実EditorWindowのマウス／DPI、実RadDollV3への新規衣装の全周fit・貫通・見た目、VRChat Build & Test／実機表示、実運用での更新・削除確認である。Core／Player／BridgeのPASSを、これらの外部受入完了へ読み替えない。
