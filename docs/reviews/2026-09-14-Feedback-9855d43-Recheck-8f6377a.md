# 9855d43レビューの現行main再照合（8f6377a）

提示されたレビューは、改名後リポジトリの基準 `9855d43` に対する衣装受け取り・材質処理の指摘である。現行 `main` の `8f6377a` へ再照合したところ、P1 3件とP2 5件は後続実装および回帰で解消済みだったため、本番コードへ重複修正は加えていない。

| 指摘 | 現行の契約・実装 | 根拠 |
|---|---|---|
| avatar移動後の衣装配置 | 衣装はavatar-root localで生成し、受け取りrootへ一度だけ親子付けする。移動・回転・scaleを二重に打ち消さない | `SkinnedClothingReceiver`、Bridge変形root回帰 |
| 更新時のownership参照喪失 | assignment identity（ObjectId）と生成object参照を分離し、同一ObjectIdのStateHash更新・削除後も割当を保持する | `NyaForgeSkinnedClothingBinding`、Bridge ownership回帰 |
| UV1の欠落 | Windows v1の出荷UVはUV0。GLB取込・semantic texture参照・GLB出力で `UNSUPPORTED_UV_SET` を返し、黙って落とさない | Core import/export回帰、GUI保存回帰 |
| sparse material slot | authored slotとdense submeshの対応を維持する | Core materialization／出力回帰 |
| MR係数 | glTF画像チャンネルをUnity用へ変換し、metallic／roughness scalarを材質へ保持する | Player／Bridge semantic texture回帰 |
| Cuff winding | Cuffの面向きを既存primitiveと同じ表裏規則へ修正する | Core topology回帰 |
| sampler共有 | 画像共有とsampler variant共有を分離し、Repeat／Clampを出力で保持する | Core GLB出力回帰 |
| 適用前・削除後の割当読込 | 割当の同一性を生成objectの存在条件から分離する | Bridge binding回帰 |

現行Coreは **500 passed / 0 failed**（artifact: `C:/Users/tomoaki/AppData/Local/Temp/NyaForge-Core-Tests-15eb0ee4b498457c9ea5ae2114c3a829`）。既存のWindows Player、外部MCP、private RadDollV3 smoke、Unity Bridgeの各証拠は `current_task.md` の直近セクションへ紐付けている。

この確認はコード／Core／Player／Bridgeの自動経路であり、実EditorWindowのマウス・DPI受入、実RadDollV3へ新規衣装を全周fitした貫通・見た目、VRChat Build & Test／実機表示を含まない。次の実装カードを増やす前に、これらの手動受入を別工程として実施する。
